<h2>note_form</h2>
<%= form_with model: [notable, @note] do |form| %>
  <% if @note.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
        <% @note.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :body, style: "display: block" %>
    <%= form.text_field :body %>
  </div>
    <div>
    <%= form.collection_select :author_id, User.all, :id, :note_title %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
